# [專案名稱] — 待辦大項目與方向決策

> 這份文件管「**想做、但還沒開規格**」的大項目，以及跨項目的方向決策。
>
> 在這之前，這類東西沒有地方放，只好塞在 AI 的 memory 裡當提醒——結果就是會過期、
> 沒人看得到、其他協作者也不知道。這是這套模式的「第 0 層」，見
> `docs/AI_CEO_WORKING_MODEL.md` §3。

## 這份文件在整套流程的哪個位置

```
ROADMAP.md          想做，還沒開規格          ← 你在這
     ↓ 決定要做，開 proposal + design + tasks
openspec/changes/   進行中的規格
     ↓ 完工，spec 升入 specs/，change 搬進 archive/
TEAM_LOG.md         每個 wave 完工紀錄
DEV_LOG.md          release 級里程碑
```

`ISSUES.md` 走另一條線——那裡管 bug 與小改，不管「還沒開始的大功能」。

---

## 維護規則

### 什麼可以寫進來

四個條件**全部**成立才寫：

1. **是功能或方向，不是 bug。** Bug 一律進 `ISSUES.md`，不管多大。
2. **還沒有 proposal。** 已經在 `openspec/changes/` 的不重複列，只在「進行中」段落留一行連結。
3. **大到需要一個 wave 以上。** 半天做得完的直接開 `ISSUES.md` 單，不必經過這裡。
4. **有人真的想做**——老闆交代的，或 CEO 提案並經老闆同意的。
   **「以後也許可以做」不算。** 那種寫進來會讓這份文件變成許願池，而許願池沒有人會看第二次。

### 什麼時候移出去

| 觸發 | 動作 |
|---|---|
| 開了 proposal | 移到「進行中」段落，只留一行連結 |
| change 完工 archive | **整條刪掉**。歷史在 `TEAM_LOG.md` / `DEV_LOG.md`，這裡不留墓碑 |
| 老闆決定不做 | 直接刪 |
| 超過幾個月沒動 | **不自動刪**，但下次 review 要問：還要嗎？ |

### 誰維護、什麼時候

- **CEO 維護。** 每次 wave 收尾寫 `TEAM_LOG` 時，順手確認這份文件的狀態還對。
- 老闆交代新的大方向 → **當場寫進來**，不要只記在 AI 的 memory 裡。
  「靠 AI 記得提醒」是把最重要的承諾託付給最不可靠的機制。
- 每條至少要有：**狀態**、**來源**（誰在什麼時候交代的）、**下一步是什麼**。
  缺「下一步」的條目等於沒寫——下次讀到的人不知道從哪裡接手。

---

## 進行中（已開 proposal）

<!-- 範例：
- **[某功能名稱]** — `openspec/changes/some-feature/` — 老闆 2026-XX-XX 交代
-->

## 想做、還沒開規格

<!-- 範例：
### 1. 反爬蟲處理
狀態：待啟動
來源：老闆 2026-XX-XX 交代「等 X 做完後要處理反爬蟲」
下一步：開 proposal，先盤點目前有哪些爬蟲行為造成問題
-->
